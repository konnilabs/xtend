# SurfaceManager Release Handoff

WP-SM-19 closes the previous release handoff by moving the SurfaceManager stack into the produktive Runtime-Linie.

The current handoff schema is `xtend.surface.runtime-release-handoff.v1`, and the local gate is:

```bash
node scripts/run_xtend_tests.js surface-runtime-release-handoff --json
```

This document remains as the bridge from the earlier release handoff to the runtime-focused handoff.
